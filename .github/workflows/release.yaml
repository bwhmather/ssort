name: Release

on:
  release:
    types:
      - published
  push:
    branches:
      - master

jobs:
  upload:
    name: "Upload to Python Package Index"
    runs-on: ubuntu-22.04
    if: success() && startsWith(github.ref, 'refs/tags')
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: 3.11
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build
    - name: Write development version
      # We use the commit count rather than the short hash to comply with the
      # scheme described in PEP-440.
      run:
        python scripts/wrap_version.py 0.0.1.dev`git rev-list --count HEAD` | src/ssort/_version.py
    - name: Build package
      run: python -m build --sdist
   # - name: Publish distribution to PyPI
   #   uses: pypa/gh-action-pypi-publish@master
   #   with:
   #     password: ${{ secrets.PYPI_API_TOKEN }}
